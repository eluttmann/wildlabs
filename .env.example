# Anthropic — server-side only. This repo is public; never commit the real key.
ANTHROPIC_API_KEY=sk-ant-...

# Chat guardrails (see apps/web/lib/rate-limit.ts)
CHAT_MAX_REQUESTS_PER_HOUR=20
CHAT_MAX_OUTPUT_TOKENS=1024
